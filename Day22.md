# Day22 - Linux 命令

## 一、文件与目录操作

### 1.1 ls - 列出目录内容

**语法**：`ls [选项] [路径]`

| 常用选项 | 作用 |
|------|------|
| `-l` | 以长格式显示（权限、属主、大小、修改时间） |
| `-a` | 显示所有文件，包括隐藏文件（以 `.` 开头） |
| `-h` | 与 `-l` 配合，以 K/M/G 人类可读方式显示大小 |
| `-R` | 递归列出子目录内容 |
| `-t` | 按修改时间排序（最新在前） |

```bash
ls -lha /home/user     # 长格式 + 隐藏文件 + 可读大小
```

**注意**：`ls -l` 第一列形如 `drwxr-xr-x`，第一个字符 `d` 表示目录、`-` 表示普通文件，后 9 位为属主/属组/其他用户的读写执行权限。

### 1.2 cd - 切换目录

**语法**：`cd [目录]`

| 用法 | 作用 |
|------|------|
| `cd /etc` | 切换到绝对路径 |
| `cd ..` | 返回上一级目录 |
| `cd ~` | 回到当前用户家目录 |
| `cd -` | 回到上一次所在目录 |

```bash
cd /usr/local && pwd    # 切换并确认当前路径
```

### 1.3 mkdir - 创建目录

**语法**：`mkdir [选项] 目录名`

| 常用选项 | 作用 |
|------|------|
| `-p` | 递归创建多级目录，目录已存在时不报错 |
| `-m` | 创建时直接指定权限 |

```bash
mkdir -p /data/logs/2026     # 一次性创建多级目录
```

### 1.4 rm - 删除文件或目录

**语法**：`rm [选项] 目标`

| 常用选项 | 作用 |
|------|------|
| `-r` | 递归删除目录及其内容 |
| `-f` | 强制删除，不提示确认 |
| `-i` | 删除前逐个询问 |

```bash
rm -rf /tmp/cache      # 递归强制删除（高危操作，慎用）
```

**注意**：`rm -rf` 删除不可恢复，无回收站；严禁对系统关键路径（如 `/etc`、`/`）执行；生产环境建议先 `mv` 到临时目录确认无误后再删。

### 1.5 cp - 复制文件或目录

**语法**：`cp [选项] 源 目标`

| 常用选项 | 作用 |
|------|------|
| `-r` | 递归复制目录 |
| `-p` | 保留文件属性（权限、时间戳） |
| `-a` | 归档复制，相当于 `-rp`（常用于备份） |
| `-i` | 覆盖前询问 |

```bash
cp -a /opt/app /opt/app_bak     # 完整备份目录
cp config.yml config.yml.bak    # 复制前先备份
```

### 1.6 mv - 重命名或者移动

**语法**：`mv [选项] 源 目标`

| 常用选项 | 作用 |
|------|------|
| `-i` | 目标存在时询问是否覆盖 |
| `-f` | 强制覆盖不提示 |
| `-u` | 仅在源文件更新时才移动 |

```bash
mv old.txt new.txt        # 重命名
mv app.jar /opt/deploy/   # 移动到目标目录
```

> mv 重命名或者移动，两者本质是同一操作：目标位置不同即为移动，同一目录下改名即为重命名。

### 1.7 tar - 打包与解包

**语法**：`tar [选项] 文件`

| 常用组合 | 作用 |
|------|------|
| `tar -czvf a.tar.gz dir` | 打包并 gzip 压缩（c 创建、z gzip、v 显示过程、f 指定文件） |
| `tar -xzvf a.tar.gz` | 解压 gzip 包（x 解包） |
| `tar -xzvf a.tar.gz -C /dir` | 解压到指定目录 |

```bash
tar -czvf backup.tar.gz /data/web     # 压缩备份
tar -xzvf backup.tar.gz -C /tmp       # 解压到 /tmp
```

**注意**：`-f` 必须是最后一个选项（后面紧跟包文件名）；解压前可用 `tar -tzvf a.tar.gz` 预览包内文件列表。

## 二、文件内容查看

### 2.1 cat - 查看完整文件内容

**语法**：`cat [选项] 文件`

| 常用选项 | 作用 |
|------|------|
| `-n` | 显示行号 |
| `-A` | 显示所有不可见字符（如 `$` 表示行尾） |

```bash
cat -n /etc/hosts        # 带行号查看
```

**注意**：文件较大时不要用 `cat` 一次看完，容易刷屏，改用 `less`/`more`/`tail`。

### 2.2 more - 分页查看

**语法**：`more 文件`

- 按 `空格` 翻下一页，按 `b` 回翻，按 `q` 退出
- 只能向下翻页，适合快速浏览

```bash
more /var/log/syslog
```

### 2.3 head - 查看文件头部内容

**语法**：`head [选项] 文件`

| 常用选项 | 作用 |
|------|------|
| `-n 行数` | 显示前 N 行，默认 10 行 |

```bash
head -n 20 /etc/profile    # 查看前 20 行
```

### 2.4 tail - 查看文件尾部内容

**语法**：`tail [选项] 文件`

| 常用选项 | 作用 |
|------|------|
| `-n 行数` | 显示末尾 N 行，默认 10 行 |
| `-f` | 实时跟踪文件新增内容（查看日志最常用） |

```bash
tail -f /var/log/nginx/access.log    # 实时滚动查看访问日志
tail -n 100 app.log                  # 查看末尾 100 行
```

**注意**：`tail -f` 用 `Ctrl + C` 退出；排查应用故障时 `tail -f` 是最常用命令之一。

## 三、文本编辑

### 3.1 vi / vim - 文本编辑器

**语法**：`vi 文件` / `vim 文件`

- vim 是 vi 的增强版（语法高亮、多级撤销），两者操作方式一致

**三种模式**：

| 模式 | 进入方式 | 作用 |
|------|------|------|
| 命令模式 | 打开文件默认进入 | 移动光标、删除、复制粘贴 |
| 插入模式 | 按 `i` / `a` / `o` 进入 | 编辑文本 |
| 末行模式 | 命令模式下按 `:` 进入 | 保存、退出、查找替换 |

**常用操作**：

| 操作 | 按键 |
|------|------|
| 进入插入模式 | `i`（光标前）、`a`（光标后）、`o`（下一行） |
| 返回命令模式 | `Esc` |
| 保存并退出 | `:wq` |
| 不保存退出 | `:q!` |
| 查找 | `/关键字`，`n` 下一个、`N` 上一个 |
| 替换 | `:%s/旧/新/g` 全文替换 |

```bash
vim /etc/nginx/nginx.conf
# 修改后按 Esc → :wq 保存退出
```

**易混淆**：`:q` 是退出（未修改时可用），`:wq` 是保存并退出，`:q!` 是放弃修改强制退出；`i` 与 `a` 的区别是插入位置在光标前还是后。

## 四、搜索

### 4.1 find - 查找文件

**语法**：`find 路径 [选项] [动作]`

| 常用选项 | 作用 |
|------|------|
| `-name` | 按文件名查找（支持通配符） |
| `-type f / d` | 按类型查找（f 文件、d 目录） |
| `-mtime +7` | 按修改时间查找（7 天前） |
| `-size +100M` | 按大小查找 |
| `-exec` | 对结果执行命令 |

```bash
find /opt -name "*.log" -type f        # 查找所有 .log 文件
find /data -mtime +30 -name "*.tmp" -exec rm {} \;   # 删除 30 天前的临时文件（慎用）
```

**注意**：`find` 按文件系统实时搜索，比 `locate` 慢但结果准确；`-exec` 中 `{}` 代表每个匹配结果，末尾 `\;` 是命令结束标志。

### 4.2 grep - 在文件中搜索文本

**语法**：`grep [选项] 关键字 文件`

| 常用选项 | 作用 |
|------|------|
| `-i` | 忽略大小写 |
| `-r` | 递归搜索目录 |
| `-n` | 显示匹配行行号 |
| `-v` | 反向匹配（排除关键字） |
| `-E` | 支持扩展正则 |

```bash
grep -rn "error" /opt/app/logs/       # 递归搜索日志中的 error
grep -v "^#" /etc/nginx/nginx.conf    # 过滤掉注释行
```

**易混淆**：`grep` 搜文件内容，`find` 按文件名找文件；管道组合 `ps -ef | grep java` 是排查进程的经典用法。

## 五、进程与环境

### 5.1 ps - 查看进程

**语法**：`ps [选项]`

| 常用选项 | 作用 |
|------|------|
| `-ef` | 全格式显示所有进程 |
| `-aux` | 显示所有含 CPU/内存占用信息的进程 |
| `-p PID` | 查看指定进程 |

```bash
ps -ef | grep java        # 查找 java 相关进程
```

**注意**：`ps` 输出是某一时刻的快照，实时刷新需用 `top`/`htop`。

### 5.2 kill - 终止进程

**语法**：`kill [信号] PID`

| 信号 | 作用 |
|------|------|
| `kill PID` | 默认发送 TERM 信号，请求正常退出 |
| `kill -9 PID` | 强制杀死进程（KILL，不可捕获） |
| `kill -15 PID` | 与默认一致，优雅终止 |

```bash
kill 12345              # 优雅终止
kill -9 12345           # 强制终止
```

**易混淆**：先 `ps -ef | grep 关键字` 拿到 PID 再 kill；`kill -9` 会跳过清理流程，可能丢数据，先试默认信号，无效再 `-9`。

### 5.3 export - 设置/导出环境变量

**语法**：`export 变量名=值`

```bash
export JAVA_HOME=/usr/local/jdk17
export PATH=$JAVA_HOME/bin:$PATH      # 将 java 加入 PATH
env | grep JAVA_HOME                  # 查看环境变量是否生效
```

**注意**：`export` 只在当前终端会话生效；要永久生效需写入 `~/.bashrc` 或 `/etc/profile` 后执行 `source ~/.bashrc`。

### 5.4 nohup - 后台运行命令

**语法**：`nohup 命令 [参数] &`

```bash
nohup java -jar app.jar > app.log 2>&1 &    # 后台启动 jar 并写日志
```

**参数说明**：
- `nohup`：忽略挂断信号（SIGHUP），关闭终端后进程仍运行
- `&`：放到后台执行
- `> app.log 2>&1`：标准输出和错误输出都重定向到日志文件

**易混淆**：只用 `&` 关终端后进程可能被杀，配合 `nohup` 才能持续运行；`2>&1` 表示把错误输出合并到标准输出，否则报错信息看不到。

## 六、重定向

### 6.1 echo - 输出内容

**语法**：`echo [选项] 内容`

| 常用选项 | 作用 |
|------|------|
| `-e` | 启用转义字符（如 `\n` 换行） |
| `-n` | 输出后不换行 |

```bash
echo "hello world"
echo -e "line1\nline2"        # 输出多行
```

### 6.2 重定向操作符

| 操作符 | 说明 | 示例 |
|------|------|------|
| `>` | 覆盖重定向：清空文件后写入 | `echo "a" > f.txt` |
| `>>` | 追加重定向：在文件末尾追加 | `echo "b" >> f.txt` |
| `2>` | 错误输出重定向 | `cmd 2> err.log` |
| `2>&1` | 错误输出合并到标准输出 | `cmd > all.log 2>&1` |

```bash
echo "hello" > file.txt      # 覆盖写入
echo "world" >> file.txt     # 追加写入
cat file.txt                 # 输出 hello / world
```

**注意**：`>` 会直接清空目标文件内容，重要文件务必先备份；`2>&1` 与 `>` 一起用时顺序为 `> 文件 2>&1`，错误信息才会一并进入文件。

## 七、网络配置

### 7.1 防火墙：开放指定端口

**firewalld（CentOS/RHEL 默认）**：

```bash
systemctl status firewalld                # 查看防火墙状态
firewall-cmd --permanent --add-port=8080/tcp    # 永久开放 8080 端口
firewall-cmd --reload                      # 重载配置生效
firewall-cmd --list-ports                  # 查看已开放端口
```

**iptables（传统防火墙）**：

```bash
iptables -I INPUT -p tcp --dport 8080 -j ACCEPT    # 放行 8080 端口
service iptables save                               # 保存规则
```

**注意**：项目部署后访问不通，优先排查防火墙端口是否开放、云安全组是否放行；只开放业务需要的端口，不开放时默认拒绝更安全。

### 7.2 反向代理：路径重写

以 Nginx 为例，将 `/api/` 请求代理到后端服务并重写路径：

```nginx
location /api/ {
    rewrite ^/api/(.*)$ /$1 break;        # 路径重写：去掉 /api 前缀
    proxy_pass http://127.0.0.1:8080;      # 反向代理到后端
    proxy_set_header Host $host;
}
```

**参数说明**：
- `rewrite ^/api/(.*)$ /$1 break;`：把 `/api/user` 重写为 `/user` 再转发
- `proxy_pass`：指定后端真实服务地址（Java 服务常为 `127.0.0.1:8080`）

**易混淆**：反向代理是客户端只面对 Nginx，由 Nginx 转发到后端（隐藏后端地址）；路径重写解决前端请求路径与后端接口路径不一致的问题。

## 今日总结

| 模块 | 核心要点 |
|------|----------|
| 文件与目录操作 | ls/cd/mkdir/rm/cp/mv（重命名或者移动）/tar，`rm -rf` 高危需谨慎 |
| 文件内容查看 | cat/more/head/tail；日志实时跟踪用 `tail -f` |
| 文本编辑 | vi/vim 三模式：命令/插入/末行；`:wq` 保存退出 |
| 搜索 | find 按文件名查找、grep 搜文件内容，可配合管道使用 |
| 进程与环境 | ps 查看、kill 终止（先优雅后 -9）；export 环境变量；nohup 后台运行 |
| 重定向 | `>` 覆盖重定向、`>>` 追加重定向，`2>&1` 合并错误输出 |
| 网络配置 | 防火墙 `firewall-cmd --add-port` 开放端口；Nginx `rewrite` + `proxy_pass` 路径重写反向代理 |
