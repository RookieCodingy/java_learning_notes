# Day30 ：微信支付、SpringTask

## 一、微信支付相关代码开发

### 1.1 需求与背景
- 微信支付这类代码开发，重点是理解**第三方接口文档怎样使用、怎样调用第三方提供的接口**，以及理解整个流程中用户、客户端、后台与第三方之间的业务逻辑传递关系。
- 第三方接口大多都是**封装为工具类**进行调用，业务代码只面向工具类编程。

### 1.2 实现方式
- 整体思路：阅读第三方接口文档 → 将接口调用封装为工具类 → 业务模块调用工具类完成支付功能。
- 代码示意（工具类封装）：
  ```java
  // 将微信支付接口封装为工具类，供业务模块调用
  public class WeChatPayUtil {

      // 调用微信支付接口，生成预订单
      public String createOrder(...) { ... }

      // 用户已支付需要退款，调用微信退款接口
      public void refund(...) { ... }
  }
  ```

### 1.3 商业门槛与替代方案
- 因为使用微信支付功能需要**商业门槛**，但后续代码模块又绕不开支付模块，可以将需要调用微信支付工具类的代码模块**注释**，通过**输出日志**的方式替代原本的支付功能调用。
- 需要替换支付功能调用的位置一共有两处：
  1. 调用微信支付接口，生成预订单
  2. 用户已支付需要退款，需要调用微信退款接口
- 代码示意（注释 + 日志替代）：
  ```java
  // 原实现：调用工具类生成预订单
  // String orderId = weChatPayUtil.createOrder(...);
  // 替代方案：注释掉原调用，用日志代替支付调用
  log.info("模拟调用微信支付接口：生成预订单");
  ```

![[Pasted image 20260825204959.png|405]]

---

## 二、SpringTask（任务调度工具）

### 2.1 概述
- **SpringTask 是任务调度工具**，可以按约定时间**自动执行代码逻辑**。

### 2.2 使用方式
- 开启任务调度：在启动类或配置类上加 `@EnableScheduling`。
- 定义定时任务：在方法上加 `@Scheduled(cron = "...")`，到约定时间自动执行。
- 代码示意：
  ```java
  @EnableScheduling // 开启任务调度
  @SpringBootApplication
  public class SkyApplication {
      public static void main(String[] args) {
          SpringApplication.run(SkyApplication.class, args);
      }
  }

  @Component
  public class OrderTask {
      @Scheduled(cron = "0 0 1 * * ?") // 每天凌晨 1 点执行
      public void checkOrder() {
          // 按约定时间自动执行的代码逻辑
      }
  }
  ```

### 2.3 cron 表达式
- **cron 表达式用于定义任务触发时间**，分为 **6-7 个域**，由空格分隔开：秒、分钟、小时、日、月、周、年（可选）。
- 各域说明：

| 域 | 含义 | 取值 |
| --- | --- | --- |
| 秒 | 秒 | 0-59 |
| 分钟 | 分钟 | 0-59 |
| 小时 | 小时 | 0-23 |
| 日 | 一个月中的第几天 | 1-31 |
| 月 | 月份 | 1-12 |
| 周 | 星期几 | 0-7（0 和 7 都表示周日） |
| 年（可选） | 年份 | 可省略 |

- 示例：`0 0 1 * * ?` 表示每天凌晨 1 点执行。

---

## 三、今日总结

| 模块 | 核心知识点 |
| --- | --- |
| 微信支付相关代码开发 | 理解第三方接口文档、封装工具类调用、商业门槛替代方案（注释 + 日志）、两处调用点（生成预订单 / 退款） |
| SpringTask | 任务调度工具、@EnableScheduling / @Scheduled、cron 表达式（6-7 个域） |
