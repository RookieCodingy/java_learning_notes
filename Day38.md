# Day38 学习笔记：秒杀下单的并发控制（超卖 / 一人一单 / 分布式锁）

> 主题：以优惠券秒杀下单为例，梳理超卖问题的成因与解决方案，以及从单体 synchronized 到分布式锁的演进链路。

## 一、超卖问题

### 1. 问题引入

秒杀场景下，大量线程并发执行"扣减库存 + 下单"。若不对共享数据做并发控制，可能出现多个线程同时读到剩余库存并都扣减成功，导致库存被扣成负数、同一份库存卖给多个用户，即**超卖问题**

### 2. 解决思路总览：悲观锁 vs 乐观锁

#### 2.1 悲观锁

- **思想**：认为线程安全问题**一定会发生**，因此在操作数据前先获取锁，确保线程**串行执行**。
- Java 中的典型实现：
  - **synchronized**：JVM 内置的隐式锁（监视器锁），可重入，代码块/方法结束时自动释放。
  - **Lock**：`java.util.concurrent.locks.Lock` 接口（如 `ReentrantLock`），需手动 `lock()` / `unlock()`，更灵活。

#### 2.2 乐观锁

- **思想**：认为线程安全问题**不一定发生**，因此不加锁，只在**更新数据时**判断是否有其他线程对数据做了修改。
  - 若数据未被其他线程修改 → 认为安全，自己执行更新；
  - 若已被其他线程修改 → 说明发生了安全问题，此时可选择**重试或抛出异常**。

#### 2.3 CAS（Compare And Swap）

- 乐观锁的底层机制：**先比较，再进行设置**。比较当前值与期望值，一致才更新，从而保证并发下的原子性。

### 3. 基于乐观锁的三种方案（扣减优惠券库存）

**方案一：使用 version 版本号控制**

- 每进行一次修改就递增一次 `version`。
- 更新时校验：若之前查询到的 `version` 与当前数据库中的 `version` 不一致，则停止修改（说明数据已被别人改过）。
- **缺点**：并发下大量请求因 version 不匹配而失败，**成功率很低**。

**方案二：利用 stock 代替 version**

- 仅在 `stock` 未发生改变时才执行售卖（即把库存本身当作版本号比较）。
- 说明：这是对 version 乐观锁的一个优化（少维护一个字段），但**并没有解决并发下成功率低的问题**。
- **缺点**：成功率依然低。

**方案三：update 时只要 stock > 0 就执行售卖**

- 把判断条件从"stock 与旧值相等"放宽为"stock > 0"。
- **优点**：既解决了超卖问题（库存为 0 时更新不成功），又**不会让请求成功率变低**（大量并发请求都能成功扣减，直到库存耗尽）。

### 4. 优惠券秒杀实现思路（乐观锁落地）

1. 先从数据库中查询该优惠券信息；
2. 若该优惠券处于**售卖期内** → 放行；
3. 若该优惠券**库存大于 0** → 放行；
4. 对数据库执行 update 扣减库存——**乐观锁的校验就放在这条 update 语句中**：
   ```sql
   UPDATE 优惠券表 SET stock = stock - 1 WHERE voucher_id = #{voucherId} AND stock > 0;
   ```
   （通过受影响行数判断是否抢购成功：返回 1 表示扣减成功，返回 0 表示库存不足、抢购失败）
5. 扣减成功后再**插入订单信息**。

---

## 二、一人一单

### 1. 需求

限制单个用户购买优惠券的额度，同一用户只能下一单。

### 2. 实现流程

1. **获取当前登录用户 userId**：通过 `UserHolder` 获取，其原理是从 **ThreadLocal** 中取出当前线程的用户信息；
2. **查询数据库订单表**：统计该 `userId` 名下该优惠券的订单数 `count`；
   - 若 `count > 0` → 说明已购买过该优惠券，直接返回、不允许再次下单；
   - 若 `count == 0` → 放行下单。

### 3. 为什么要加锁？

多线程下会出现竞态：前一个线程刚好**完成下单前**（还未插入订单），另一个线程此时查询到 `count` 仍为 0，也进入下单流程，最终导致**同一用户购买多单 → 一人一单被破坏（相当于该用户的"超卖"）**。

结论：**必须加锁**，才能实现多线程下的一人一单。

### 4. 选择什么锁？

- **乐观锁不适用**：乐观锁适用于"更新已存在的数据"；而这里是**插入操作**，没有合适的字段（如 version）来实现 CAS 判断。
- 该场景**冲突概率较高**，适合使用**悲观锁**（synchronized）。
- 加锁范围：利用 `synchronized` 锁住"从查到插"的流程——即从"判断该 userId 条件下订单 count"到"插入数据"的整段逻辑，可**提取为一个方法**并在该方法上加锁。

### 5. 如何保证锁住的是同一个 userId？（锁粒度问题）

- 若直接以 userId 对象作为锁对象，是不可靠的：每个线程拿到的 `userId.toString()` 结果**并不是指向同一个对象**（不同 String 实例），锁就互不相干。
- 解决：将 userId **先 `toString()` 再调用 `intern()`**，使其放入**字符串常量池**中；相同 userId 的字符串会复用**同一个引用对象**，从而保证不同线程真正锁住同一把锁。

### 6. 内部方法调用导致事务失效（锁、事务与代理对象）

**问题**：在同一个类中，一个方法调用另一个声明了 `@Transactional` 的方法时，**事务会失效**。

**原因**：Spring 中声明式注解（@Transactional）的原理是使用**代理对象**；而类内部方法互相调用时默认使用 `this` 调用，**绕开了代理对象**，因此事务注解不生效。

**解决**：显式获取并使用代理对象来调用，即调用 AOP 工具类 `AopContext.currentProxy()` 取得当前类的代理对象，再通过代理对象调用事务方法。

---

## 三、从单体到集群：synchronized 失效与分布式锁

### 1. 集群下单时 synchronized 锁不住了！

**现象**：当服务使用**集群部署**时，即使加了 synchronized，依然会发生同一用户超卖。

**为什么锁不住？**

- synchronized 属于 **JVM 内置锁，只能锁住同一个 JVM 进程内的线程**；
- 集群中每个节点的锁是**相互独立**的。假设有两个集群节点，同时都接收到同一用户的请求，各自只会锁住**本 JVM 内**的线程，两个节点上的下单流程仍可并发执行，依然会造成同一用户的超卖。

### 2. 分布式锁

- 需求特性：**多进程可见**（跨 JVM/跨节点生效）、**互斥**、**高可用**、**安全性**（如防误删、防死锁、可重入等）。
- Redis 实现：可利用 `SETNX`（SET if Not eXists）命令实现分布式锁。

---

## 四、复习自检清单（待巩固问题）

- Spring 声明式事务的原理是什么？
- 悲观锁 synchronized 锁的使用细节？
- 锁 userId 时为什么要 `.toString().intern()`？
- 锁、事务、代理对象三者之间的关系？
- 集群场景下为什么需要分布式锁？分布式锁需要满足哪些特性？
