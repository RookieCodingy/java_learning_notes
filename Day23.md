# Day23 - 苍穹外卖（项目入门）

## 一、软件开发流程

苍穹外卖项目遵循完整的软件开发流程：

需求分析 → 产品原型 → 设计（UI 设计、数据库设计、接口设计）→ 编码 → 测试 → 上线运维

| 阶段 | 说明 |
|------|------|
| 需求分析 | 明确业务需求与功能范围 |
| 产品原型 | 输出产品原型，确认页面与交互 |
| 设计 | UI 设计（界面）、数据库设计（表结构）、接口设计（前后端约定） |
| 编码 | 后端 Java + 前端页面开发实现 |
| 测试 | 功能测试、前后端联调 |
| 上线运维 | 部署到服务器、运行监控与维护 |

## 二、项目分层对象

苍穹外卖项目使用三层数据对象：

| 对象 | 全称 | 作用 |
|------|------|------|
| Entity | 实体 | 与数据库表结构一一对应 |
| DTO | 数据传输对象 | 封装前端提交到后端的请求数据 |
| VO | 视图对象 | 封装后端返回给前端的展示数据 |

**使用场景**：
- 前端传参 → 封装为 DTO → Controller 接收
- 后端查询 → 封装为 VO → 返回给前端展示
- Entity 只负责与数据库表映射，不在请求/响应中直接暴露

## 三、Nginx 反向代理与负载均衡

### 3.1 反向代理

Nginx 接收客户端请求，通过 `proxy_pass` 转发到后端服务：

```nginx
server {
    listen 80;
    location / {
        proxy_pass http://webservers;
    }
}
```

### 3.2 负载均衡

通过 `upstream` 定义一组后端服务器，Nginx 将请求分发到不同服务器：

```nginx
upstream webservers {
    server 192.168.1.101:8080;
    server 192.168.1.102:8080;
}
```

**说明**：`upstream` 定义服务器组，`proxy_pass` 指定转发目标；配合使用可实现反向代理 + 负载均衡，隐藏后端真实地址。

## 四、登录功能完善 - MD5 加密

- 用户密码若以明文存储，数据库泄露后密码直接暴露，存在安全隐患
- 使用 **MD5 加密** 对密码进行加密后再存储与校验

**思路**：
- 注册/修改密码时：明文密码 → MD5 加密 → 存入数据库
- 登录校验时：用户输入密码 → MD5 加密 → 与数据库密文比对

## 五、Swagger 接口文档

- 后端接口开发完成后，通过 Swagger 自动生成接口文档
- 可视化展示接口地址、请求参数、返回结果，方便前后端联调与测试

## 今日总结

| 模块 | 核心要点 |
|------|----------|
| 软件开发流程 | 需求分析 → 产品原型 → 设计（UI/数据库/接口）→ 编码 → 测试 → 上线运维 |
| 项目分层对象 | Entity 对应数据库表；DTO 封装请求数据；VO 封装响应数据 |
| Nginx | 反向代理 `proxy_pass`；负载均衡 `upstream` 定义服务器组 |
| 登录安全 | MD5 加密存储密码，登录时加密比对 |
| 接口文档 | Swagger 自动生成接口文档，辅助前后端联调 |
